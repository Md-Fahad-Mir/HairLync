# Dependencies & build output (rebuilt inside the image)
node_modules
dist
.next
out

# Secrets — never bake these into an image
.env
.env.*

# VCS / tooling / docs
.git
.gitignore
.dockerignore
Dockerfile
README.md
*.log
.DS_Store
npm-debug.log*
yarn-error.log*
