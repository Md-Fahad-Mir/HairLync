# ═══════════════════════════════════════════════════════════════════════
# HairIQ — Backend Environment Variables Template
# Copy this file and fill in real values. NEVER commit the filled copy.
# ═══════════════════════════════════════════════════════════════════════

# Django Core
DEBUG=False
DJANGO_SECRET_KEY=generate-a-strong-secret-key-here
DJANGO_ALLOWED_HOSTS=api.hairiq.io,localhost,127.0.0.1
DJANGO_DEBUG=False

# PostgreSQL
DB_ENGINE=django.db.backends.postgresql
DB_NAME=hairiq
DB_USER=hairiq_admin
DB_PASSWORD=your-strong-db-password
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432

# Email (SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@hairiq.io
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@hairiq.io

# AI Service Integration
AI_API_URL=http://ai:8001/analyze

# AWS S3 (optional — for production media storage)
USE_S3=True
AWS_STORAGE_BUCKET_NAME=hairiq-media-prod
AWS_S3_REGION_NAME=your-region
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# CORS
CORS_ALLOWED_ORIGINS=https://hairiq.io,https://admin.hairiq.io,http://localhost:3000,http://localhost:3001

# Django Superuser (initial setup only)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@hairiq.io
DJANGO_SUPERUSER_PASSWORD=use-a-strong-password
