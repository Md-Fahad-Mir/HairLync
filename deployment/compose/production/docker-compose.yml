# ─── Production Compose ───────────────────────────────────────────────
# Usage: docker compose -f ../docker-compose.base.yml -f docker-compose.yml up -d
#
# Pulls pre-built images from Docker Hub (pushed by CI/CD).

services:
  ai:
    image: ${REGISTRY}/hairiq-ai:${IMAGE_TAG:-latest}
    env_file:
      - ../environments/.env.production.ai

  backend:
    image: ${REGISTRY}/hairiq-backend:${IMAGE_TAG:-latest}
    env_file:
      - ../environments/.env.production.backend
    restart: always

  landing:
    image: ${REGISTRY}/hairiq-landing:${IMAGE_TAG:-latest}
    env_file:
      - ../environments/.env.production.landing

  admin:
    image: ${REGISTRY}/hairiq-admin:${IMAGE_TAG:-latest}
    env_file:
      - ../environments/.env.production.admin
