# Copy to terraform.tfvars and fill in. (terraform.tfvars is gitignored.)
# The bucket name must be GLOBALLY unique across all of AWS — add random chars.

project_name      = "hairiq"
region            = "eu-north-1"
aws_profile       = "default"
state_bucket_name = "hairiq-tfstate-CHANGEME1234"
