---
handlers:
  - name: restart docker compose
    shell: |
      docker compose -f docker-compose.prod.yml pull --ignore-pull-failures || true
      docker compose -f docker-compose.prod.yml up -d --force-recreate --remove-orphans || true
    args:
      chdir: "{{ project_path }}"
    become_user: ubuntu
