- hosts: webservers
  become: true

  vars_files:
    - ../group_vars/all.yml
    - "../group_vars/{{ env }}.yml"

  roles:
    - role: docker
    - role: nginx
    - role: app-deploy
    - role: certbot
